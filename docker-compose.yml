version: "3.3"

services: 

  mysql:
    image: mysql:5.7
    container_name: api-mysql
    volumes:
      - ./db:/docker-entrypoint-initdb.d/
    ports: 
      - 3306:3306
    environment:
      - MYSQL_USER=root
      - MYSQL_ROOT_PASSWORD=h4BDXiTH2c9Nn3jN
  
  redis:
    image: redis:5.0.3-alpine
    container_name: api-redis
    command: redis-server --requirepass WD3h1QhzOfM06l56
    ports: 
      - 6379:6379